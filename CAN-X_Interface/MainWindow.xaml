<Window x:Name="MainWindow1" x:Class="CAN_X_CAN_Analyzer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CAN_X_CAN_Analyzer" 
        xmlns:components="clr-namespace:CAN_X_CAN_Analyzer.Components"
        mc:Ignorable="d" 
        Title="CAN-X" WindowStartupLocation="CenterScreen" Margin="0" Height="735.5" Width="1186" Closing="Window_Closing" Loaded="MainWindow1_Loaded" ResizeMode="CanResizeWithGrip">
    <Window.DataContext>
        <local:ComPortViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <Style TargetType="{x:Type TextBlock}" x:Key="MyStyle">
            <Setter Property="Background">
                <Setter.Value>
                    <SolidColorBrush Color="Transparent"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="Binding.TargetUpdated">
                    <BeginStoryboard HandoffBehavior="Compose">
                        <Storyboard TargetProperty="(TextBlock.Background).(SolidColorBrush.Color)">
                            <ColorAnimation Duration="0:0:0.3" To="LightGreen" AutoReverse="True"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>
        <ControlTemplate x:Key="RoundedButtonTemplate" TargetType="{x:Type Button}">
            <Border x:Name="border" 
                    CornerRadius="10" 
                    BorderBrush="{TemplateBinding BorderBrush}" 
                    BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                <ContentPresenter x:Name="contentPresenter" ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" ContentStringFormat="{TemplateBinding ContentStringFormat}" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsDefaulted" Value="True">
                    <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" TargetName="border" Value="#FFBEE6FD"/>
                    <Setter Property="BorderBrush" TargetName="border" Value="#FF3C7FB1"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" TargetName="border" Value="#FFC4E5F6"/>
                    <Setter Property="BorderBrush" TargetName="border" Value="#FF2C628B"/>
                </Trigger>
                <Trigger Property="ToggleButton.IsChecked" Value="True">
                    <Setter Property="Background" TargetName="border" Value="#FFBCDDEE"/>
                    <Setter Property="BorderBrush" TargetName="border" Value="#FF245A83"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" TargetName="border" Value="#FFF4F4F4"/>
                    <Setter Property="BorderBrush" TargetName="border" Value="#FFADB2B5"/>
                    <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="#FF838383"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
        <ControlTemplate x:Key="RoundedToggleButtonTemplate" TargetType="{x:Type ToggleButton}">
            <Border x:Name="border" 
                    CornerRadius="10" 
                    BorderBrush="{TemplateBinding BorderBrush}" 
                    BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                <ContentPresenter x:Name="contentPresenter" ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" ContentStringFormat="{TemplateBinding ContentStringFormat}" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
            </Border>
            <ControlTemplate.Triggers>
                <!--<Trigger Property="IsDefaulted" Value="True">
                    <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                </Trigger>-->
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" TargetName="border" Value="#FFBEE6FD"/>
                    <Setter Property="BorderBrush" TargetName="border" Value="#FF3C7FB1"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" TargetName="border" Value="#FFC4E5F6"/>
                    <Setter Property="BorderBrush" TargetName="border" Value="#FF2C628B"/>
                </Trigger>
                <Trigger Property="ToggleButton.IsChecked" Value="True">
                    <Setter Property="Background" TargetName="border" Value="#FFBCDDEE"/>
                    <Setter Property="BorderBrush" TargetName="border" Value="#FF245A83"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" TargetName="border" Value="#FFF4F4F4"/>
                    <Setter Property="BorderBrush" TargetName="border" Value="#FFADB2B5"/>
                    <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="#FF838383"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <DockPanel Grid.ColumnSpan="3" Margin="0,0,0,25">
            <StackPanel x:Name="Menu_Bar"   DockPanel.Dock="Top" Height="25" Margin="0">
                <Menu Height="21" VerticalAlignment="Top" Margin="0,0,0,10">
                    <MenuItem x:Name="MenuItemFile" Header="File">
                        <MenuItem x:Name="MenuItemNew" Header="New" Click="MenuItemNew_Click"/>
                        <MenuItem x:Name="MenuItemOpenProject" Header="Open Project" Click="MenuItemOpenProject_Click"/>
                        <MenuItem x:Name="MenuItemSaveProject" Header="Save Project" Click="MenuItemSaveProject_Click"/>
                        <Separator/>
                        <MenuItem x:Name="MenuItemExit" Header="Exit" Click="MenuItemExit_Click"/>
                    </MenuItem>
                    <MenuItem x:Name="MenuItemAbout" Header="About" Click="MenuItemAbout_Click"/>
                </Menu>
            </StackPanel>
            <StackPanel x:Name="COM_Port"  DockPanel.Dock="Right" Width="192" Background="#FFEEEEEE">
                <StackPanel Height="25" Margin="0,5,0,5">
                    <Label x:Name="LabelConnectionStatus" FontSize="14" FontFamily="Arial Rounded MT Bold" Content="Press Connect Button"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5,0,10 " HorizontalAlignment="Center">
                    <Button x:Name="ButtonConnect" Margin="0,0,10,0" Click="ButtonConnect_Click" Template="{StaticResource RoundedButtonTemplate}" Background="{x:Null}" BorderBrush="{x:Null}">
                        <Image Source="Resources/connect.png" Width="25" IsEnabled="False" />
                    </Button>
                    <Button x:Name="ButtonDisconnect" Margin="10,0,0,0" Click="ButtonDisconnect_Click" Template="{StaticResource RoundedButtonTemplate}" Background="{x:Null}" BorderBrush="{x:Null}" IsEnabled="False">
                        <Image Source="Resources/disconnect.png" Width="25" />
                    </Button>
                </StackPanel>
                <StackPanel Margin="0,0,0,15">
                    <StackPanel>
                        <ComboBox x:Name="ComboBoxCOM" SelectedIndex="0" ItemsSource="{Binding AvailablePorts}" DisplayMemberPath="FullName" SelectedValuePath="PortName" SelectedValue="{Binding SelectedComPort}"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel>
                    <GroupBox Header="Baud" BorderBrush="#FFABAFB2" Margin="0,0,0,10">
                        <StackPanel Margin="0,10,0,10">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <ComboBox x:Name="ComboBoxAPB1" Margin="0" VerticalAlignment="Top" Width="100" SelectionChanged="ComboBoxAPB1_SelectionChanged" SelectedIndex="0" HorizontalAlignment="Right" IsEnabled="False" />
                                <Label Content="APB1 Clock" />
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">

                                <ComboBox x:Name="ComboBoxBaudRate" Margin="0" VerticalAlignment="Top" SelectionChanged="ComboBoxBaudRate_SelectionChanged" Width="75" HorizontalAlignment="Right"/>
                                <Label Content="Baud Rate (Kbit/s)" />
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,5" HorizontalAlignment="Left">
                                <ComboBox x:Name="ComboBoxMode" HorizontalAlignment="Left" SelectionChanged="ComboBoxMode_SelectionChanged" >
                                    <ComboBoxItem Content="Normal" IsSelected="True"/>
                                    <ComboBoxItem Content="Loopback"/>
                                    <ComboBoxItem Content="Listen"/>
                                </ComboBox>
                                <Label Content="CAN Mode" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <TextBox x:Name="TextBoxBtrValue" MaxLength="10" Height="23" Margin="0" TextWrapping="Wrap" Text="0x001A001D" VerticalAlignment="Top" Width="120" HorizontalAlignment="Left" IsEnabled="False"/>
                                <Label Content="BTR Reg" />
                            </StackPanel>
                            <Button x:Name="ButtonBtrValue" Content="Update" HorizontalAlignment="Center" VerticalAlignment="Top" Width="120" Margin="0,10,0,0" Click="ButtonBtrValue_Click" Template="{StaticResource RoundedButtonTemplate}">
                                <Button.ToolTip>
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold" FontSize="14">
                                            Update BTR
                                        </TextBlock>
                                        <TextBlock>
                                            Sends the BTR value and CAN mode to the CAN device
                                        </TextBlock>
                                    </StackPanel>
                                </Button.ToolTip>
                            </Button>

                        </StackPanel>

                    </GroupBox>
                </StackPanel>
                <GroupBox Header="Misc" Height="113" BorderBrush="#FFABAFB2">
                    <StackPanel Margin="10,10,0,0">
                        <CheckBox x:Name="CheckBoxBlind" Content="Large Text" Click="CheckBoxBlind_Click" />
                        <StackPanel Orientation="Horizontal">
                            <RadioButton Content="Hex" GroupName="DataType" IsChecked="True" />
                            <RadioButton Content="Binary" GroupName="DataType" IsEnabled="False" />
                            <RadioButton GroupName="DataType" Content="Decimal" IsEnabled="False" />
                        </StackPanel>
                        <CheckBox x:Name="CheckBoxAscii" Content="ASCII" Click="CheckBoxAscii_Click" />
                        <CheckBox x:Name="CheckBoxNotes" Content="Notes" Click="CheckBoxNotes_Click" />
                    </StackPanel>
                </GroupBox>
                <GroupBox Header="Auto Tx" BorderBrush="#FFABAFB2">
                    <StackPanel Margin="0,10,0,20">
                        <ToggleButton x:Name="toggleButtonAutoTx" Width="60" Margin="20,0,0,0" Content="AutoTx" Click="ToggleButtonAutoTx_Click" Template="{StaticResource RoundedToggleButtonTemplate}" Height="20" IsEnabled="True">
                            <ToggleButton.ToolTip>
                                <StackPanel>
                                    <TextBlock FontWeight="Bold" FontSize="14">
                                        Auto Transmit
                                    </TextBlock>
                                    <TextBlock>
                                        Automatically transmit messages in list that are checked
                                    </TextBlock>
                                </StackPanel>
                            </ToggleButton.ToolTip>
                        </ToggleButton>
                    </StackPanel>

                </GroupBox>

            </StackPanel>

            <TabControl >
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border Name="Border" BorderThickness="1,1,1,1" BorderBrush="Black" CornerRadius="5,5,0,0" Margin="0,0" >
                                        <ContentPresenter x:Name="ContentSite"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        ContentSource="Header"
                                        Margin="10,2"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="LightSkyBlue" />
                                            <Setter TargetName="Border" Property="BorderThickness" Value="1,1,1,1"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="False">
                                            <Setter TargetName="Border" Property="Background" Value="#FFD1D1D1" />
                                            <Setter TargetName="Border" Property="BorderThickness" Value="0,0,0,0"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>
                <TabItem Header="Message Monitor">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="35"/> <!-- Buttons -->
                            <RowDefinition Height="*"/> <!-- Receive -->
                            <RowDefinition Height="5"/> <!-- grid splitter -->
                            <RowDefinition Height="*"/> <!-- Transmit -->
                        </Grid.RowDefinitions>
                        <StackPanel x:Name="Buttons" Grid.Row="0" Orientation="Horizontal" Margin="20,0,0,0" RenderTransformOrigin="0.492,1.483" Grid.ColumnSpan="2">
                            <Button x:Name="ButtonClear" Content="Clear" Width="75" Margin="0,0,10,0" Click="ButtonClear_Click" Height="20" Template="{StaticResource RoundedButtonTemplate}"/>
                            <Button x:Name="ButtonSaveRxMessages" Content="Save Messages" Width="91" Margin="30,0,0,0" Click="ButtonSaveRxMessages_Click" Height="20" MinWidth="100" Template="{StaticResource RoundedButtonTemplate}">
                                <Button.ToolTip>
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold" FontSize="14">
                                          Save Messages
                                        </TextBlock>
                                        <TextBlock>
                                          Save messages to a CSV file
                                        </TextBlock>
                                    </StackPanel>
                                </Button.ToolTip>
                            </Button>
                            <!--<Button x:Name="ButtonLoadMessages" Content="Load Messages" Margin="10,0,0,0" Height="20" MinWidth="100" />-->
                            <ToggleButton x:Name="ButtonPauseMessages" Content="Pause" Margin="10,0,0,0" Click="ButtonPauseMessages_Click" Height="20" MinWidth="70" Template="{StaticResource RoundedToggleButtonTemplate}" IsEnabled="False">
                                <ToggleButton.ToolTip>
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold" FontSize="14">
                                            Pause Scrolling
                                        </TextBlock>
                                        <TextBlock>
                                            This pauses the scrolling but new messages are still added.
                                        </TextBlock>
                                    </StackPanel>
                                </ToggleButton.ToolTip>
                            </ToggleButton>

                            <ToggleButton x:Name="ButtonScrollMessages" Content="Scroll" Margin="10,0,0,0" Click="ButtonScrollMessages_Click" Height="20" MinWidth="70" Template="{StaticResource RoundedToggleButtonTemplate}"/>
                        </StackPanel>

                        <components:MessageMonitor Grid.Row="1" x:Name="messageMonitor"/>

                        <GridSplitter Grid.Row="2" Background="#FF80B3FD" HorizontalAlignment="Stretch" Margin="0" />

                        <components:TransmitMessages Grid.Row="3" x:Name="transmitMessages"/>
                    </Grid>
                </TabItem>
                <TabItem Header="Message Editor">
                    <Grid Margin="0,0,0,0">
                        <TabControl TabStripPlacement="Top">
                            <TabControl.Resources>
                                <Style TargetType="TabPanel">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                </Style>
                            </TabControl.Resources>
                            <TabItem x:Name="TabItemReceiveMessages" Header="Receive Messages">
                                <components:EditRxMessages x:Name="editRxMessages"/>
                            </TabItem>
                            <TabItem x:Name="TabItemTransmitMessages" Header="Transmit Messages">
                                <components:EditTxMessages x:Name="editTxMessages"/>
                            </TabItem>
                        </TabControl>
                    </Grid>
                </TabItem>
            </TabControl>
        </DockPanel>
        <StatusBar x:Name="StatusBar" Height="25" VerticalAlignment="Bottom" Margin="0,0,10,0" Background="#FFE2E2E2" Grid.ColumnSpan="3">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="180" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="110" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="300" />
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>
            <StatusBarItem >
                <TextBlock x:Name="StatusBarStatus" Foreground="Red" FontWeight="Bold" Margin="15,0,0,0" MinWidth="600" Height="25"/>
            </StatusBarItem>
            <Separator Grid.Column="1" />
            <StatusBarItem Grid.Column="2">
                <TextBlock x:Name="StatusBarStatusHardware" Margin="0,0,20,0" />
            </StatusBarItem>
            <Separator Grid.Column="3" />
            <StatusBarItem Grid.Column="4">
                <TextBlock x:Name="StatusBarStatusVersion" Margin="0,0,20,0" />
            </StatusBarItem>
            <Separator Grid.Column="5" />
            <StatusBarItem Grid.Column="6">
                <ProgressBar x:Name="ProgressBar" RenderTransformOrigin="0.5,0.5" Height="20" Width="300" BorderBrush="#FF7C7C7C" Background="#FFCFCFCF" Margin="0" Maximum="5000" />
            </StatusBarItem>
            <StatusBarItem Grid.Column="6">
                <TextBlock x:Name="TextBoxBufferPercentage" Background="{x:Null}" MinWidth="300" MinHeight="20" TextAlignment="Center"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
